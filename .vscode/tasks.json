{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Win64 build",
            "type": "shell",
            "command": "gcc -o CrossBreed main.c",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}